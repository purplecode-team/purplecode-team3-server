scalar DateTime
type Query {
    user: [User]
}
type Mutation {
    uploadProduct(
        idCategory: Int!
        title: String!
        description: String
        startPrice: Int!
        bidPrice: Int!
        startDate: DateTime!
        endDate: DateTime!
        images: [String!]!
    ): Product!
}